#include "main.h"

/** _islower - check for a long lowercase character 
 * @c: character to check
 *
 * Return 1 if lowercase, 0 otherwise
 */
int _islower(int c)
{ 
	if (c >= 'a' && c <= 'z')
	{
		return(1);
	}
	retunr (0);
}
